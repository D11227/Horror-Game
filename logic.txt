- Chọn ngẫu nhiên một vị trí quả bóng
- Vd: nếu có 20 quả bóng, random chọn đại một quả bóng trong 20 quả
- Tạo một array
let ghost = ['1','2',...];
=> let i = Math.floor(Math.random() * 20);
=> console.log(ghost[i]);

+ Khi click một quả bóng không phải bóng ma thì sẽ biến mất
+ Khi click một quả bóng ma thì sẽ hiện Pop Up
